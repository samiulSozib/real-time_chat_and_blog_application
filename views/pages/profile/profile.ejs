<%- include('../../partials/header.ejs') %> 

    <div>
        <div class="container">
            <h4>My Profile (<%= profile.name %> )</h4>
            <div class="row">
                <div class="col-lg-3 col-md-4 col-sm-12">
                    <div class="card card-body">
                        <img class="img-thumbnail img-fluid" id="avatar" src="/uploads/default.png" alt="avatart">
                        <p class="mt-4">Name: <strong><%= profile.name %></strong> </p>
                        <p class="mt-4">Email: <strong><%= profile.email %></strong> </p>
                        <p class="mt-4">Bio: <strong><%= profile.bio %></strong> </p>
                        <button class="btn btn-info">Edit Profile</button>
                    </div>
                </div>
                <div class="col-lg-9 col-md-8 col-sm-12 bg-secondary">profile</div>
            </div>
        </div>
    </div>

    <%- include('../../partials/footer.ejs') %> 

    </body>
</html>